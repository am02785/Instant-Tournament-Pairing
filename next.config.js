/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
  
    images: {
      domains: [], // Add domains here if needed for next/image
    },
  };
  
  module.exports = nextConfig;